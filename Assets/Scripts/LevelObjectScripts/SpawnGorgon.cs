using UnityEngine;

public class SpawnGorgon : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //todo: spawn gorgon enemy
        
        Destroy(gameObject);
    }

}
